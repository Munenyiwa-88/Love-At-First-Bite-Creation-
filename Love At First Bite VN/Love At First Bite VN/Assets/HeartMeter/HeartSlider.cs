using JetBrains.Annotations;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

/*did not use this, as I realised that beacuse my slider is connected to the dialogue, I would then have to use
 the dialogue script, so this coding is in the dialogueManager script.*/
/*public class HeartSlider : MonoBehaviour
{
    public int heart;
    public int maxHeart;
    public Slider slider;



    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        heart = maxHeart;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
*/